[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python manage.py migrate && gunicorn pragma.wsgi:application --bind 0.0.0.0:$PORT --workers 2"
healthcheckPath = "/usuario/login/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"